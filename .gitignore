# Build / packaging
build/
dist/
*.egg-info/
*.egg
wheels/

# Python bytecode + caches
__pycache__/
*.py[cod]
*$py.class
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Virtual envs
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# codegraph runtime state (don't ship a user's index in this repo)
.codegraph/

# codegraph auth key (never commit)
.codegraph/auth.key

# Internal material: design proposals, benchmarks and the release
# runbook are maintainer-only, not published.
internal/

# Maintainer-only project skill (reads the internal runbook)
.claude/skills/release/

# Local model weights (GGUF), never committed
downloads/
